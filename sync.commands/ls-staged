#!/home/dafrito/projects/backup-settings/backup
case "$1" in
	-q|--quiet) 
		[ -s $SESSION/sync/staged ]
		exit ;;
esac
[ -f $SESSION/sync/staged ] && cat $SESSION/sync/staged
# vim: set filetype=sh :
